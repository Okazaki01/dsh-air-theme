# INSTALL-MANUAL.md — 手动安装说明（给用户看，照着做就行）

> 不需要懂电脑，不需要装任何东西。全程 4 步，约 2 分钟。
> 当前版本 **v0.2.5**。

---

## 第 1 步：解压

把 `dsh-air-theme-v0.2.5.zip` 解压，得到 `dsh-air-theme` 文件夹
（里面能看到 `安装.bat`、`INSTALL-MANUAL.md` 就对了）。

> 可选（有 Node.js 的话，10 秒确认包是好的）：在该文件夹里打开命令行，输入
> `node scripts/check-release.mjs`。看到 `结论: 三类历史故障的修复全部在位 ✓`
> 才是完整的好包。

## 第 2 步：安装（二选一，选一个做就行）

**方式 A（最简单，推荐）**：双击文件夹里的 **`安装.bat`**。
- 会弹出一个黑色窗口，等它显示「安装完成」，然后关掉窗口。

**方式 B（电脑装了 Node.js 的话）**：
1. 在 `dsh-air-theme` 文件夹里打开命令行（地址栏输入 cmd 回车）；
2. 输入 `node scripts/install.mjs` 回车。

脚本会自动找到你的 EAC 安装位置和 profile，把皮肤同步过去并写入启用配置（重复执行也安全）。

## 第 3 步：生效

**完全退出 DeepSeek Harness EAC**（右下角托盘图标也要退出），再重新打开。

> ⚠️ **第一次安装时，只按 Ctrl+R 刷新页面是不够的。** 插件列表是应用启动时读的，
> 必须真正重启一次。之后再改皮肤内容才不需要重启。

## 第 4 步：确认成功

打开后看这几处（都对上就是成功了）：

| 看哪里 | 应该看到 |
| --- | --- |
| **屏幕右下角** | 短暂出现「**AIR·夏日青空 主题已生效 ✓ v0.2.5**」（约 8 秒后消失） |
| **背景（浅色 + 深色）** | 变成观铃拥抱夏日青空的图。**切到深色模式后也还在**（不是一片纯黑） |
| **左上角** | 金色 **`deepseek`** 字样 + 紧跟一个深蓝小胶囊里的白色 **`HARNESS`** |
| **输入框** | 四角有金色弧线、上边框中央有金色宝石 |
| **新会话界面** | 小鲸鱼图标旁边有标题「将未完的夏天，寄往天空的尽头」，**两者在同一水平线上** |
| **新会话输入框位置** | 输入框大致在**竖直中间、略偏下**，不是被压在窗口最底部 |
| **滚动** | 在有消息的会话里滚动滚轮，**输入框始终固定在底部不动** |
| **悬停提示** | 鼠标移到模型输出框底下的小按钮（复制 / 分享）上，黑色提示框**完整可见**，没被输入框挡住或切掉 |
| **重启后（重要）** | **完全退出再打开** → 进新会话界面：小鲸鱼 / 文字横幅 / 预览版贴标**仍然同一条水平线**；再切一次深色模式，**背景图也还在**。旧版是「热重载就正常、冷启动必坏」，v0.2.4 / v0.2.5 才彻底修好 |

## 如果没生效（按顺序试）

0. **先做一次体检**（有 Node.js 的话）：在文件夹里开命令行输入 `node scripts/doctor.mjs`。
   它会告诉你皮肤装没装、启用行开没开、以及**你这个环境**下一步该做什么。
1. 打开 **设置 → 皮肤**，点「**AIR·夏日青空**」启用（皮肤之间互斥，别的皮肤要关掉）；
2. **再次完全退出再打开**（网页版：关掉 `dsh web` 重新跑一次，再刷新浏览器页面）；
3. 还不行：重新双击一次 `安装.bat`，再重启。

## 我不是用 EAC 的（纯网页版 / 别的桌面端）

一样能用，皮肤本身跟用哪个客户端没关系。区别只有两点：

| 你的情况 | 装法 | 生效 |
| --- | --- | --- |
| EAC 桌面端 | 双击 `安装.bat` | 完全退出 EAC（含托盘）再打开 |
| 纯网页版（自己跑 `dsh web`） | 命令行里 `node scripts/install.mjs --profile web` | 关掉 `dsh web` 重新跑一次，再刷新浏览器页面 |
| 别的桌面端 | `node scripts/install.mjs --profile <它的profile名>` | 重启那个客户端 |

- **不知道 profile 叫什么**：跑 `node scripts/doctor.mjs`，它会列出来并标出正在跑的是哪个。
- **没有 Node.js**：先装一个（nodejs.org，选 LTS 一路下一步），或者找会用命令行的人帮你跑一次。
- 一般只有「网页版」这一种情况需要你手动加 `--profile`，其余情况直接双击 `安装.bat` 就行。

## 卸载 / 换回默认皮肤

打开 **设置 → 皮肤**，点「默认」即可切回，皮肤随时可换。

## v0.2.3 / v0.2.4 / v0.2.5 修了什么

- 左上角 `deepseek` / `HARNESS` 标识不再消失；
- 新会话标题文字会正常出现，小鲸鱼与标题对齐；
- 滚动滚轮时输入框不再跟着移动、也不再"到顶弹回"；
- 新会话界面的输入框位置改回"居中偏下"，不再被压在窗口最底部；
- 模型输出框底下小按钮的黑色悬停提示不再被切掉；
- **（v0.2.4）冷启动后新会话界面的小鲸鱼 / 横幅 / 预览版贴标不再错位** ——
  上一版有个坑：只有在「热重载一次」之后才正常，**完全退出再打开就会错位**；
- **（v0.2.5）深色模式下背景图不再消失** —— 上一版有个坑：冷启动后切深色，
  整个窗口会变成一片纯黑（热重载一次又好了）。

逐条说明（含实测数据）见包内 **`CHANGELOG.md`**。
如果你拿到的是 v0.1.1 / v0.2.0 / v0.2.1 / v0.2.2 / v0.2.3 / v0.2.4，上面几处问题会存在，请换 **v0.2.5**。
